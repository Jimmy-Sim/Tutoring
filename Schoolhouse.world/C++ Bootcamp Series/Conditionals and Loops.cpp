#include <iostream>

using namespace std;

int main() {
  int number;
  cin >> number;
  // Conditions:
  // Equal to:
  ==
          // Greater than:
          >
              // Less than:
              <
              // Greater than or equal to:
              >=
              // Less than or equal to:
              <=
              // Multiple conditions:
              // And:
              && or
      and
      // Or:
      || or or

      // If:
      if (condition) {
    action;
  }
  // Else if:
  else if (condition) {
    action;
  }
  // Else:
  else {
    action;
  }

  // Loops:
  // For loop:
  for (int i = 0; i < quantity; i++) {
    action;
  }
  // While loop:
  while (condition) {
    action;
  }
}
