/*
Different time complexities in Big O:
  - O(1)
  - O(N)
  - O(N^2)
  - O(2^N)
  - etc.
*/


// Examples of different time complexities:

// O(N):
for (0 ... N) {
    ...
}

// O(N^2):
for (0 ... N) {
    for (0 ... N) {
        ...
    }
}


// A few tips for competitive programming:

// Fast input and output:
ios_base::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);

// Include every library in C++:
#include <bits/stdc++.h>
